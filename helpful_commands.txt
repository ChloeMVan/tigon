kills ivshmem and qemu

python3 /users/Chloevan/tigon/emulation/vm_lib/start_vm.py clean_ivshmem --shmem_dir /mnt/cxl_mem

# Kill existing QEMU and ivshmem so start_vm doesn’t refuse to start
sudo pkill -9 qemu-system
sudo pkill -9 ivshmem
sleep 3

# If you use ivshmem and had /mnt/cxl_mem mounted:
sudo python3 emulation/vm_lib/start_vm.py clean_ivshmem --shmem_dir /mnt/cxl_mem
sudo umount /mnt/cxl_mem 2>/dev/null || true

# needed imports
sudo apt-get update
sudo apt-get install -y libjemalloc-dev
sudo apt-get install python3-pip
sudo pip3 install pyroute2
sudo apt-get install mkosi
sudo apt-get install -y cargo
sudo apt-get install -y rustc 
sudo apt-get install -y libguestfs-tools

# other installations, sometimes not needed
sudo apt-get install numactl
sudo apt-get install libgoogle-glog0v5 libgflags2.2
sudo apt-get install cmake build-essential
sudo apt-get install clang-15
sudo apt install libgoogle-glog-dev libgflags-dev
sudo apt-get install libboost-dev libboost-all-dev
sudo apt-get install lld-15

#github
ssh-keygen -t ed25519 -C “cvancaemelbecke@gmail.com”
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
<copy and paste to github ssh keys>
ssh -T git@github.com

# connect to github
git clone git@github.com:ChloeMVan/tigon.git

sudo ./emulation/start_vms.sh --using-old-img --cxl 0 5 2 1 1

# example run
./scripts/run.sh YCSB TwoPLPasha 2 3 rmw 300000 50 0.7 10 1 0 1 Clock OnDemand 200000000 1 WriteThrough None 30 10 GROUP_WAL 20000 0 0